# @mdehub/react

```ts
import { useImageCompressor } from '@mdehub/react'
```

`useImageCompressor(options?)` returns:

- `compress(input)`
- `abort()`
- `progress`
- `status`: `idle | running | success | error`
- `error`
- `result`

Peer: `react >= 18`.
